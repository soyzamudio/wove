---
import Layout from "@theme/Layout.astro";
import PostCard from "@theme/PostCard.astro";
import { getSettings, listPosts } from "../../lib/api";

const { slug } = Astro.params;
const settings = await getSettings();
const cursor = Astro.url.searchParams.get("cursor") ?? undefined;
const { items, nextCursor } = await listPosts({ term: slug, limit: settings.postsPerPage, cursor });
---

<Layout settings={settings} title={`Tag: ${slug}`} path={Astro.url.pathname + Astro.url.search}>
  <h1 class="page-title">Tag: {slug}</h1>
  {items.length === 0 ? (
    <p>Nothing tagged "{slug}" yet.</p>
  ) : (
    <ul class="post-list">
      {items.map((post) => <PostCard post={post} />)}
    </ul>
  )}
  {nextCursor && (
    <p class="pagination">
      <a href={`/tag/${slug}?cursor=${encodeURIComponent(nextCursor)}`}>Older posts →</a>
    </p>
  )}
</Layout>
